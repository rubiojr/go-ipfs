#!/bin/sh
set -e

mkdir -p /var/lib/ipfs

if [ "$1" = "configure" ]; then
  addgroup --quiet ipfs 
  adduser --disabled-password --gecos ipfs --quiet --ingroup ipfs ipfs
  chown -R ipfs:ipfs /var/lib/ipfs
fi
